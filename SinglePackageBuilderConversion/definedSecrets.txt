TEMP_BIOC_TOKEN
repository → Settings → Secrets and variables → Actions → New repository secret:

  - results of setting up a personal access token on someones account with access to target cloning repo
          This personal access token:
          settings -> developers settings -> personal access tokents -> fine-grained tokens
    		 access on tempbioc
		 repository access (all repos in org)
		 repository permissions read to metadata
		 repository permissions read/write to admin,contents,workflows
		 organiztion permissions: read



EXCLUDED_REVIEWERS
repository → Settings → Secrets and variables → Actions → New repository secret:

  - a comma-separated list of usernames you want to temporarily skip NO SPACES!, e.g.:
    lgatto,DarioS,PeteHaitch,LiNk-NY,vjcitn,jianhong,stemangiola,hpages,Bisaloo,mireia-bioinfo,HelenaLC,js2264,cvanderaa,almeidasilvaf,hyu-kim



ADMIN_REVIEWERS
repository → Settings → Secrets and variables → Actions → New repository secret:

  - a comma-separated list of usernames you want 
    jwokaty,lshep



BIOC_ORG_TOKEN
repository → Settings → Secrets and variables → Actions → New repository secret:
  - results of personal access toekn on someones account with access to org level
  access at github.com/Bioconductor
          This personal access token:
          settings -> developers settings -> personal access tokents -> fine-grained tokens
    		 access on Bioconductor
		 repository access (all repos in org)
		 repository permissions read to metadata
		 repository permissions read/write to admin, contents, and issues
		 organization level - members read		 
		 

